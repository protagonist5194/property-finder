<nav class="col-md-2 d-none d-md-block bg-light sidebar">
  <div class="sidebar-sticky">
    <ul class="nav flex-column">
      <%= render "layouts/navbar/#{ current_account.admin? ? 'admin' : 'user' }" %>
    </ul>
    <h6 class="sider-heading d-flex justify-content-between align-items-center" px-3 mt-4 mb-1 text-muted>
      <span>Settings</span>
    </h6>

    <ul class="nav flex-column mb-2">
      <li class="nav-item">
        <%= link_to profile_path(current_account), class: "nav-link" do %>
          <i data-feather="file-text"></i>
          My profile
        <% end %>
      </li>
             
      <li class="nav-item">
        <%= link_to edit_account_registration_path, class: "nav-link" do %>
          <i data-feather="settings"></i>
          Account Settings
        <% end %>
      </li>    
    </ul>
  </div>
</nav>